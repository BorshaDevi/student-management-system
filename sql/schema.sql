CREATE TABLE students(
    student_id BIGINT PRIMARY KEY,
    name VARCHAR (350),
    age SMALLINT ,
    gender VARCHAR(20),
    department VARCHAR(150),
    email VARCHAR(250),
    phone_number INT,
    address VARCHAR(400)
)